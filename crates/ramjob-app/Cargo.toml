[package]
name = "ramjob-app"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
ramjob-core = { path = "../ramjob-core" }
tauri = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
