# 45 — Panel backstop warning + SPEC

**Milestone:** M4 · Plan Task 6  
**Depends on:** 43

## Goal
§7.4 warning when enabling always_enforce; SPEC already partially updated — ensure panel copy + progress ledger.

## Verify
`node --check crates/ramjob-app/ui/app.js`
